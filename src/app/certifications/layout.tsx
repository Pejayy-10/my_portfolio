import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Certifications | Fran Peruso",
  description:
    "Fran Peruso's professional certifications, credentials, and continuing education achievements.",
};

export default function CertificationsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
